# 📅 2026-04-10

## 오늘 푼 문제

### 최댓값 구하기 (프로그래머스)
- 유형: 집계 함수 (Aggregate)
- 핵심
  -  MAX(): 컬럼의 최댓값 반환
- 실수: 없음

[풀이]
```sql
SELECT
    MAX(DATETIME)
FROM ANIMAL_INS;
```

### 최솟값 구하기 (프로그래머스)
- 유형: 집계 함수 (Aggregate)
- 핵심:
  -  MIN(): 컬럼의 최솟값 반환
- 실수: 없음

[풀이]
```sql
SELECT
    MIN(DATETIME)
FROM ANIMAL_INS;
```
